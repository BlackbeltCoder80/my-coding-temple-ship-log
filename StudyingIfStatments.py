# Defining a variable and setting it to True
torch_lit = True

# We are writing an if statment. If torch_lit is True, we want the print to run
if torch_lit == True:
    print("Venture forth into the cave!")
    